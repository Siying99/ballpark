# Ballpark entry: Auclert, Rognlie, Souchier, and Straub (2021)

> Structured brief for coding agents (Claude Code, Cursor, etc.). Human-facing content lives in [`OpenHA.md`](OpenHA.md).

> **Scope of the formalization layer in this directory: household block + aggregate block (full baseline).** The companion `dolo-plus-draft.yaml` encodes the household three-stage period (`shocks` → `cons` → `disc`) of `bellman-excerpt.md` §8. As of the June 2026 follow-up PR, `aggregate-draft.yaml` now encodes the aggregate / GE closure of §§3–4 (the 16-equation system E.1–E.16 of §7, grouped into six blocks), and `openha-ge-draft.yaml` wires the two into a general-equilibrium composition. The canonical record of the scope history (why household-only was right for PR #73, the follow-up blueprint, and the aggregate-YAML syntax workaround) lives in [`bellman-excerpt.md` §13.1 and §13.5](bellman-excerpt.md). `OpenHA.md` frontmatter records the scope in its `formalization_scope` field (`household-and-aggregate`). The aggregate YAML is the Cursor/Claude *Extract* draft; the Matsya `evaluate` pass on it is the remaining human-in-the-loop step (see "Common next tasks").

## Paper

- **Citation:** Auclert, Adrien; Rognlie, Matthew; Souchier, Martin; and Straub, Ludwig (2021), "Exchange Rates and Monetary Policy with Heterogeneous Agents: Sizing up the Real Income Channel," NBER Working Paper 28872.
- **DOI:** [10.3386/w28872](https://doi.org/10.3386/w28872) · also on [SSRN](https://papers.ssrn.com/sol3/papers.cfm?abstract_id=3856853).
- **Core model:** Small-open-economy New Keynesian model with a heterogeneous-agent, incomplete-markets household block. Households hold a single real asset $a$ subject to a borrowing limit and face uninsurable idiosyncratic productivity $e_{it}$ following a first-order Markov chain. The household's only decision-perch control is aggregate real consumption $c_t$ (the CES home/foreign split is mechanical given relative prices; hours $N_t$ are union-set); savings follow from the budget identity $a_{t+1} = m_t - c_t$.
- **Why in-ballpark:** The paper's central decomposition, $d\mathbf{C} = -\tfrac{\alpha}{1-\alpha}\mathbf{M}\,d\mathbf{Q} + \mathbf{M}\,d\mathbf{Y}$, rests on the matrix $\mathbf{M}$ of intertemporal MPCs — exactly the sequence-space Jacobian object HARK's incomplete-markets household block already exposes. OpenHA is therefore the cleanest open-economy mapping for HARK.

## If a user asks to work on this item

1. **Read first:** [`bellman-excerpt.md`](bellman-excerpt.md) — self-contained statement of the entire baseline model (Sections 2–4 of the paper): comprehensive symbol table; full portfolio-form household problem and its reduction to canonical one-asset form; aggregate closure (CPI, real exchange rate, real UIP, RoW demand and monetary policy, law of one price, production, sticky-wages Phillips curve, monetary rule, NFA / current account); intertemporal MPC apparatus and chain-rule decomposition of $\mathbf{M}^{\text{HA-IM}}$; analytical shock-response results (international Keynesian cross, $\chi=1$ neutrality, contractionary-depreciation threshold, monetary-policy decomposition, "stealing demand from the future"); SolvingMicroDSOPs-style three-stage decomposition of the household block (`shocks-only` → `cons-noshocks` → `disc`) with explicit perch table and between-period connector $a^p_{t+1}=(1+r_t)\,\psi_t$; consolidated baseline equation system; calibration values from paper Table 2; and an extensions register listing Section 5's quantitative-model additions.
2. **Then:** [`OpenHA_summary.ipynb`](OpenHA_summary.ipynb) → "The model" for the human-facing prose summary. The notebook and `bellman-excerpt.md` agree on effective controls, cash-on-hand, and borrowing-limit handling for the household block.
3. **Paper source for AI ingestion:** neither `OpenHA.mmd` (Pandoc-converted paper) nor `OpenHA.pdf` is yet committed in this directory. Use the paper at <https://web.stanford.edu/~aauclert/ha_oe.pdf> (August 2024 revision; DOI `10.3386/w28872`) as the authoritative source, and treat completing the asset layer as one of the "Common next tasks" below.

## Formalization status

- Explicit recursive formulation: **present** in `OpenHA_summary.ipynb` → "Domestic (Brazil) households" (effective control $c$; cash-on-hand $m_t$; productivity $e_{it}$ as a Rouwenhorst-discretized AR(1) Markov chain; borrowing limit $\underline{a}$).
- [`bellman-excerpt.md`](bellman-excerpt.md): **committed and expanded**. As of April 2026 it covers the full baseline model (not only the household block) — see the "If a user asks" section above for contents. The household-block stage decomposition (now §8) is unchanged in substance; only notation was sharpened to match the paper's $a^p$ vs $a'$ distinction, and the between-period connector was corrected from $a_{t+1}=\psi_t$ to $a^p_{t+1}=(1+r_t)\psi_t$. **Section 13 ("Open items") is the canonical record** of the household-only YAML scope, the Rouwenhorst-constructor workaround, and the explicit horizon choice — all three documents (`bellman-excerpt.md`, `OpenHA.md`, this `AGENTS.md`) agree, and the YAML carries inline pointers back to §13 at the points of divergence.
- [`dolo-plus-draft.yaml`](dolo-plus-draft.yaml): **committed and finalised** (household three-stage period: `shocks` → `cons` → `disc`, with between-period connector $a^p_{t+1}=(1+r_t)\,\psi_t$, EGM channel exposed in `cons`, and paper-grounded calibration block). Generated by Matsya in session `topics2026-siying99-ballpark` turns 7–9 (April 2026) and verified against the paper. The YAML preamble carries `horizon: infinite-stationary` (consistent with the paper's continuum-of-households stationary problem; see `bellman-excerpt.md` §13.3) and an inline `# unresolved:` flag at the `e_prime` declaration recording the Rouwenhorst-constructor workaround (see `bellman-excerpt.md` §13.2).
- [`aggregate-draft.yaml`](aggregate-draft.yaml): **committed (June 2026 follow-up PR).** Encodes the aggregate / GE closure E.1–E.16 (`bellman-excerpt.md` §7) in six blocks — `prices_fx` (E.3–E.7), `firms` (E.8/E.10/E.13), `wage_phillips` (E.9), `monetary` (E.11–E.12), `goods_market` (E.2), `nfa_ca` (E.14–E.16) — plus `unknowns`/`targets` for the sequence-space GE solve and a paper-Table-2 calibration block. The container syntax (block-system / `unknowns` / `targets` / `terminal_conditions`) is **SPECULATIVE** — dolo-plus has no canonical aggregate-block idiom; flagged with a block-level `# unresolved (SPECULATIVE block-level):` comment and per-item inline flags. Canonical record: `bellman-excerpt.md` §13.5. **This is the Cursor/Claude Extract draft; the Matsya `evaluate` pass on it is still pending** (see "Common next tasks").
- [`openha-ge-draft.yaml`](openha-ge-draft.yaml): **committed (June 2026 follow-up PR).** GE composition wiring `dolo-plus-draft.yaml` (household) and `aggregate-draft.yaml` (aggregate): declares both as `components`, the bidirectional `wiring` ((r,w,N) → household → (C,A) → aggregate → (r,w,N)), the equilibrium selection rule (Q∞=Q_ss, nfa∞=0), the MIT-shock drivers (eps, B), and the steady state. Same SPECULATIVE-syntax caveat and §13.5 record as the aggregate YAML.
- [`OpenHA-perch-graphs.md`](OpenHA-perch-graphs.md): **committed**. Mermaid perch-graph diagrams for both the household three-stage period (encoded in YAML) and the aggregate-block 16-equation system (specified in `bellman-excerpt.md` §§3, 7, not yet in YAML). Mermaid diagrams render directly on GitHub.
- [`bellman-excerpt.pdf`](bellman-excerpt.pdf): **committed**. Pandoc-rendered PDF of `bellman-excerpt.md` for offline reading and assignment-deliverable archival.
- [`verification.md`](verification.md): **committed and updated** with a record of the April 2026 paper re-read, the `bellman-excerpt.md` expansion, the round-8 iterative YAML construction with Matsya, and the round-9 instructor-review revisions (scope restriction, inline `# unresolved:` migration, horizon declaration, asset-layer artifacts).
- [`matsya-session.txt`](matsya-session.txt): **committed** — session `topics2026-siying99-ballpark` (now 9 turns; latest round produced the finalised household YAML).

## Known model features requiring attention in a formalization pass

> All four items below have a corresponding entry in `bellman-excerpt.md` §13 ("Open items"); that section is the canonical record. The YAML carries inline `# unresolved:` flags (where applicable) at the points of divergence, with cross-references back to §13.

- **Aggregate block: now encoded (June 2026 follow-up PR), with SPECULATIVE container syntax.** The aggregate / GE closure (home-goods market clearing, wage Phillips curve, real UIP, monetary rule, NFA, current account) is encoded in `aggregate-draft.yaml` and wired in `openha-ge-draft.yaml`. dolo-plus has **no canonical syntax** for an aggregate sequence-space block or for HA-household ⊕ aggregate GE composition, so the container keywords are SPECULATIVE (the *economics* — the 16 equations of `bellman-excerpt.md` §7 — is final and paper-grounded). Canonical record of the syntax workaround: `bellman-excerpt.md` §13.5. The **Matsya `evaluate` pass on the aggregate block is the open item** — the YAML is currently the Cursor/Claude Extract draft only.
- **Horizon: explicit declaration.** `dolo-plus-draft.yaml` now carries `horizon: infinite-stationary` in its preamble. The model is the infinite-horizon stationary problem of paper §2 (continuum of households, no death, no birth, fixed Markov productivity process); a terminal-period stage is therefore not present and is not appropriate. See `bellman-excerpt.md` §13.3 for the paper-grounded justification.
- **Rouwenhorst constructor syntax (non-blocking workaround).** The Markov shock is declared as `@dist MarkovChain(Pi, e)` with $\Pi$ assumed to be built externally from $(\rho_e, \sigma_\varepsilon, n_e)$ via Rouwenhorst. A higher-level `@dist Rouwenhorst(rho_e, sigma_eps, n_e)` constructor would be cleaner but is not yet canonical in the dolo-plus spec. The conservative form is flagged with an inline `# unresolved: see bellman-excerpt.md §13.2` comment **at the point of divergence** in the YAML (the `e_prime` declaration in Stage 1, per CONTRIBUTING.md line 44 — "workarounds are flagged inline at the point of divergence, not collected at the bottom of the file"). Canonical record: `bellman-excerpt.md` §13.2.
- **Productivity process added by formalizer, not stated in ballpark prior to this pass.** The original ballpark notebook did not specify the stochastic structure of $e_{it}$. As documented in `verification.md`, this was **edited in** from the paper, which states a first-order Markov chain with $\mathbb{E}\,e_{it}=1$, calibrated as an AR(1) in log income (cross-sectional std $\sigma_e=0.883$, persistence $\rho_e=0.912$) and Rouwenhorst-discretised. The YAML uses the *innovation* std $\sigma_\varepsilon=\sigma_e\sqrt{1-\rho_e^2}=0.3620$.
- **Time-varying equilibrium prices declared as parameters.** The household YAML declares `w`, `N`, `r` as `parameters` rather than as inputs from an aggregate stage. This is the *correct* representation under the household-only scope (no aggregate stage exists in this YAML for them to come from), not a workaround — see `bellman-excerpt.md` §13.4. The follow-up `aggregate-draft.yaml` would supply them as time-varying exogenous inputs to the household stage.

## Common next tasks (grounded)

1. **Run the Matsya `evaluate` pass on the aggregate block** (continue session `topics2026-siying99-ballpark`). `aggregate-draft.yaml` + `openha-ge-draft.yaml` were added in the June 2026 follow-up PR as the Cursor/Claude *Extract* draft (CONTRIBUTING.md "formalization iteration" step 1). The remaining steps 2–4 — feed the aggregate YAML + `bellman-excerpt.md` §§3, 7 to Matsya, collect its flagged gaps, and accept/edit/reject each against the paper — are not yet done. Expected friction: Matsya will likely flag the SPECULATIVE container kinds (`aggregate-block-system`, `ge-composition`) and the real-UIP placement / terminal-condition idioms (see `bellman-excerpt.md` §13.5). Record the round in `verification.md` (a Round-11 entry) with the accept/edit/reject discipline.
2. **(Optional) Implement the household block in HARK.** With `dolo-plus-draft.yaml` finalised and calibration paper-grounded, a HARK `IndShockConsumerType` instantiation matching this YAML's parameters is a natural cross-check; the iMPC matrix $\mathbf{M}$ from such an instantiation can be directly compared to the paper's Figure 5.
3. **(Optional) Formalize one or more Section 5 extensions.** `bellman-excerpt.md` §9 lists six (Stone-Geary preferences, unequal incidence, sticky home prices, imperfect pass-through, delayed substitution, inertial Taylor rule) with the specific equations needed for each.
4. **(Optional) Re-run Matsya `evaluate` on the full expanded `bellman-excerpt.md`** in session `topics2026-siying99-ballpark` to surface any remaining DDSL issues with sections beyond the household block (§§3, 5, 6, 7, 9). The household block already passed Matsya verification in round 8; the natural trigger for this re-run is when the aggregate-block YAML of (1) is being drafted.
5. **(Optional) Local `OpenHA.mmd` for AI ingestion.** Per the new repo-root `CONTRIBUTING.md` Asset-layer table, `*.mmd` is gitignored — produce a local `OpenHA.mmd` from the paper PDF via Mathpix or pandoc for AI-assisted reading; do not commit. The paper PDF itself is not redistributed in this directory; the authoritative source is at <https://web.stanford.edu/~aauclert/ha_oe.pdf> (DOI `10.3386/w28872`).

## Workflow reminders

- **Matsya session:** continue `topics2026-siying99-ballpark` for any further Matsya calls on this item. Do **not** open a new session — session string is committed in [`matsya-session.txt`](matsya-session.txt).
- **Paper verification:** Matsya output must be checked against the paper PDF (or a local `.mmd`), not only against the ballpark notebooks. `verification.md` explicitly references the paper at <https://web.stanford.edu/~aauclert/ha_oe.pdf>.
- **When flagging workarounds in YAML:** use inline `# workaround:` or `# unresolved:` comments **at the point of divergence** (per CONTRIBUTING.md line 44), not collected in a tail block. `dolo-plus-draft.yaml` currently carries one such inline flag at the `e_prime` Markov-chain declaration (Rouwenhorst constructor syntax; cross-references `bellman-excerpt.md` §13.2). Each inline `# unresolved:` should also have a corresponding numbered entry under `bellman-excerpt.md` §13 so that there is a single canonical record across the three documents (excerpt, YAML, AGENTS.md).
